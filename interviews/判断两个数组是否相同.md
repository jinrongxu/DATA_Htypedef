```js
const arr1=[1,2,2,3,3,1]
const arr2=[2,2,1,1,3,3]
function group(arr){
    const newArr=[];
    arr.forEach(item=>{
        let result=newArr.find(val=>val.includes(item))
        if(result){
            
        }
    })
}
```